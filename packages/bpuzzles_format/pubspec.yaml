name: bpuzzles_format
description: Shared streaming package format for Better Puzzles catalogs.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.12.1

dependencies:
  crypto: ^3.0.7

dev_dependencies:
  lints: ^6.0.0
  test: ^1.29.0
